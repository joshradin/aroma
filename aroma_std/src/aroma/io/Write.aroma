namespace aroma.io
import aroma.io.IOException

#[lang = "Write"]
/// Write interface
public interface Write {

    /// Writes values from the given buffer
    fn write(buffer: Array[out Char]) -> Int throws IOException;
}